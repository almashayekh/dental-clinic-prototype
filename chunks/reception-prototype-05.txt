"card pad"><h3 style="margin:0 0 5px">${p.name}</h3><div class="sub">${p.phone}</div><div class="moneyline"><span>الرصيد</span><strong>${money(p.balance)}</strong></div><div class="actions" style="margin-top:12px"><button class="btn sm primary" onclick="openProfile(${p.id})">فتح الملف</button><button class="btn sm" onclick="editPatient(${p.id})">تعديل</button><button class="btn sm" onclick="bookPatient(${p.id})">حجز</button></div></div>`).join("")}
function openPatientForm(){editingPatient=null;pfTitle.textContent="مريض جديد";["pfName","pfPhone","pfDob","pfNational","pfGeneral"].forEach(id=>document.getElementById(id).value="");pfGender.value="";go("patient-form")}
function editPatient(id){const p=P(id);editingPatient=id;returnTo="patients";pfTitle.textContent="تعديل بيانات المريض";pfName.value=p.name;pfPhone.value=p.phone;pfGender.value=p.gender||"";pfDob.value=p.dob||"";pfNational.value=p.national||"";pfGeneral.value=p.general||"";go("patient-form")}
function savePatient(){if(!pfName.value.trim()||!pfPhone.value.trim()){toast("الاسم والموبايل مطلوبان");return}const obj={name:pfName.value.trim(),phone:pfPhone.value.trim(),gender:pfGender.value,dob:pfDob.value,national:pfNational.value,general:pfGeneral.value};if(editingPatient)Object.assign(P(editingPatient),obj);else{currentPatient=Date.now();patients.push({id:currentPatient,...obj,balance:0})};toast("تم حفظ بيانات المريض");if(returnTo==="appointment"){fillPatientSelect();apptPatient.value=currentPatient;go("appointment")}else go("patients")}
function returnPatientForm(){go(returnTo==="appointment"?"appointment":"patients")}
function bookPatient(id){newAppointment();apptPatient.value=id}
function openProfile(id){currentPatient=id;go("profile")}
function editCurrentPatient(){returnTo="profile";editPatient(currentPatient)}
function bookCurrent(){bookPatient(currentPatient)}
function renderProfile(){const p=P(currentPatient);prName.textContent=p.name;prPhone.textContent=p.phone;prBalance.textContent=money(p.balance);const next=appointments.find(a=>a.patientId===p.id&&["Scheduled","Arrived","With Dentist"].includes(a.status));prNext.textContent=next?`${next.time} • ${next.doctor}`:"لا يوجد";const lp=payments.filter(x=>x.patientId===p.id).sort((a,b)=>b.date.localeCompare(a.date))[0];prLastPay.textContent=lp?`${lp.date} • ${money(lp.amount)}`:"لا يوجد";const tl=timelines[p.id]||[{title:"ملف المريض",text:"لا توجد أحداث سابقة في النموذج"}];prTimeline.innerHTML=tl.map(e=>`<div class="event"><h4>${e.title}</h4><p>${e.text}</p></div>`).join("")}
function checkout(id){currentAppt=id;currentPatient=A(id).patientId;const a=A(id),p=P(currentPatient);coPatient.textContent=`${p.name} • ${p.phone}`;coItems.innerHTML=(a.items||[{name:a.reason,price:a.visitTotal||0}]).map(x=>`<div class="moneyline"><span>${x.name}</span><strong>${money(x.price)}</strong></div>`).join("");coOld.textContent=money(p.balance);const due=(a.visitTotal||0)+(p.balance||0);coDue.textContent=money(due);coPay.value=due;receipt.classList.add("hidden");previewPay();go("checkout")}
function previewPay(){const a=A(currentAppt),p=P(currentPatient),due=(a.visitTotal||0)+(p.balance||0),pay=Number(coPay.value||0);coRemain.textContent=money(Math.max(0,due-pay))}
function recordPayment(){const a=A(currentAppt),p=P(currentPatient),due=(a.visitTotal||0)+(p.balance||0),pay=Number(coPay.value||0);if(pay<0||pay>due){toast("قيمة الدفع غير صحيحة");return}const rem=due-pay;p.balance=rem;a.paid=rem===0;payments.push({id:Date.now(),date:coDate.value,time:new Date().toLocaleTimeString("en-US",{hour:"2-digit",minute:"2-digit",hour12:false}),patientId:p.id,amount:pay,method:coMethod.value,remaining:rem});receipt.innerHTML=`تم تسجيل ${money(pay)} • المتبقي ${money(rem)} <button class="btn sm" onclick="window.print()">طباعة الإيصال</button>`;receipt.classList.remove("hidden");toast("تم تسجيل الدفعة")}
function showReport(t){dailyTab.classList.toggle("active",t==="daily");periodTab.classList.toggle("active",t==="period");dailyReport.classList.toggle("hidden",t!=="daily");periodReport.classList.toggle("hidden",t!=="period");if(t==="daily")renderDailyReport();else renderPeriodReport()}
function renderDailyReport(){const list=payments.filter(x=>x.date==="2026-09-13");const sum=m=>list.filter(x=>!m||x.method===m).reduce((s,x)=>s+x.amount,0);rdTotal.textContent=money(sum());rdCash.textContent=money(sum("نقدي"));rdCard.textContent=money(sum("بطاقة"));rdTransfer.textContent=money(sum("تحويل"));dailyPayments.innerHTML=list.map(x=>`<tr><td>${x.time}</td><td>${P(x.patientId).name}</td><td>${money(x.amount)}</td><td>${x.method}</td><td>${money(x.remaining)}</td></tr>`).join("")}
function renderPeriodReport(){const from=rfFrom.value,to=rfTo.value,m=rfMethod.value;const list=payments.filter(x=>x.date>=from&&x.date<=to&&(m==="all"||x.method===m));const total=list.reduce((s,x)=>s+x.amount,0);rpTotal.textContent=money(total);rpCount.textContent=list.length;rpAvg.textContent=money(list.length?total/list.length:0);rpOutstanding.textContent=money(patients.reduce((s,p)=>s+p.balance,0));periodPayments.innerHTML=list.sort((a,b)=>b.date.localeCompare(a.date)).map(x=>`<tr><td>${x.date}</td><td>${x.time}</td><td>${P(x.patientId).name}</td><td>${money(x.amount)}</td><td>${x.method}</td></tr>`).join("")}
function toast(m){const t=document.getElementById("toast");t.textContent=m;t.classList.add("show");clearTimeout(window.tt);window.tt=setTimeout(()=>t.classList.remove("show"),2000)}
fillPatientSelect();renderSchedule();renderPatients();renderDailyReport();
</script>
</body></html>